Browser-based IRC client implementation. Work in progress.

The backend in `server` maintains persistent connections to IRC servers -- the
frontend in `client` exposes that functionality through the browser.